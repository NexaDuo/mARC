name = "design"
description = "mARC frontend and user experience specialist."
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = "Discover the repository UI conventions, implement the requested flow, and validate the affected user behavior with the available checks."
